# Path to checkpoint from models/train.py (relative to backend/: ../models/checkpoints/...)
# Recommended after evaluation: EfficientNet-B0 (see docs/model_evaluation.md)
DERMASSIST_CHECKPOINT=../models/checkpoints/best_binary_efficientnet_b0_fold0.pt
# If unset or invalid, `/predict` uses placeholder inference.
